# Local development (SQLite)
DATABASE_URL="file:./dev.db"
AUTH_SECRET="replace-with-long-random-secret"
APP_BASE_URL="http://localhost:3000"
PRISMA_SCHEMA_PATH="prisma/schema.prisma"
REGISTRATION_MODE="open"
# Comma separated static invite codes, used when REGISTRATION_MODE="invite" (optional fallback)
# Managed one-time invite codes are created inside the app at /invites.
INVITE_CODES=""
# Admin users (emails, comma-separated). Only admins can manage /invites in production.
ADMIN_EMAILS=""

# Production/Staging (Neon Postgres)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
# AUTH_SECRET="at-least-32-characters-random-secret"
# APP_BASE_URL="https://your-domain.example"
# PRISMA_SCHEMA_PATH="prisma/schema.postgres.prisma"
# REGISTRATION_MODE="invite"
# INVITE_CODES="code1,code2"

# Planned next phase (object storage)
# S3_BUCKET=""
# S3_REGION=""
# S3_ACCESS_KEY_ID=""
# S3_SECRET_ACCESS_KEY=""
